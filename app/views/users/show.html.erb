<%= @user.name %>, <%= @user.email %>  <!--7.4: ユーザー情報を表示するための仮のビュー-->

<% provide(:title, @user.name) %>      <!--7.8: ユーザー表示ビューに名前とGravatarを表示-->
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div> 